using UnityEngine;
using System.Collections;

public class PoliceScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
				//if (player ActivateTrigger && Player In crime && raytrace){
				//nodes.policeSee = true
				//}
				//else If(player ActivateTrigger && nodes.policemode == 2 && raytrace)
				//nodes.policeSee = true
				//		}
				//if(nodes.seeLast == true)
				//NewBehaviourScript.agent.SetDestination(PlayerStore.PlayerLoc);
		//if(nodes.policemode == 3 && player touces close-by trigger)
		// game over
	
	}
}
